test:
  post:
    - npm run build

deployment:
  push_to_cosmos:
    branch: document-mgt
    commands:
      - rsync -avz --rsh=­"npm install && npm run build" -e * circleci@64.80.61.75:/Library/Server/Web/Data/Sites/cosmos
