test:
  post:
    - npm run build

deployment:
  push_to_cosmos:
    branch: database
    commands:
      - rsync -avz --exclude=node_modules * circleci@64.80.61.75:/Library/Server/Web/Data/Sites/cosmos-dev
      - ssh -v circleci@64.80.61.75 'source ~/.bash_profile && cd /Library/Server/Web/Data/Sites/cosmos-dev/ && npm install && npm run build'
