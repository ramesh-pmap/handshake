test:
  post:
    - npm run build

deployment:
  push_to_cosmos:
    branch: document-mgt
    commands:
      - rsync -avz --exclude=node_modules * circleci@64.80.61.75:/Library/Server/Web/Data/Sites/cosmos
      - ssh circleci@64.80.61.75 "cd /Library/Server/Web/Data/Sites/cosmos/ && npm install && npm run build"
